# HANDOFF — 이어서 작업하려면 여기부터

마지막 갱신: 2026-08-21

## 30초 요약

SOC SHIFT:30은 **공개 주소에서 처음부터 끝까지 동작합니다.** MEMO까지
사람이 돌려보고 확인했습니다.

**게임 규칙은 지금 막 확정됐습니다.** 마지막 세 가지가 규칙을 건드렸습니다.

1. **메모 로그** — 받은 공지가 닫아도 사라지지 않고 쌓입니다. 연결 경보를
   최대 17초 뒤에 만나는데 다시 볼 방법이 없던 문제를 없앴습니다
2. **결과 화면의 공지 연결** — 판정 기록 항목 안에 그 경보를 돕던 공지를
   붙입니다. 틀린 경보 밑에 "그 정보를 갖고 있었다"가 드러납니다
3. **경보 제한시간 1400 → 2000 → 3000ms** — 하루에 두 번 올랐습니다.
   1400은 읽는 데만 다 쓰였고, **2000을 배포한 뒤에도 동기들에게서
   "읽을 시간이 촉박하다"는 피드백이 와서** 3000으로 다시 올렸습니다.
   사실 1줄당 500ms → 750ms입니다. 대신 한 판이 15장에서 10장이 됩니다.
   근거는 `notes/05-difficulty-experiment.md`

**그리고 08-20에 판이 멈추는 버그를 하나 잡았습니다.** 메모가 떠 있는 동안
`A`·`D`를 누르면 그 경보가 판정도 만료도 되지 않고 근무 시계만 흘렀습니다.
공개본에 들어 있던 버그이고, **수정과 검사는 끝나 커밋돼 있습니다.**

같은 날 오후에 화면 쪽이 더 붙었습니다. 규칙은 건드리지 않았습니다.

- 가이드가 6쪽이 되고, 설정에 소리 크기 5단계와 조작키 안내가 생겼습니다
- 로비·일시정지·결과·모달을 방향키로 고를 수 있습니다
- 캐릭터 초상 8종이 메모 발신자와 경보 당사자에 붙었습니다

**08-21에 상사의 전화가 들어갔습니다.** `GAME_SPEC` 14절 전체 구현입니다.
21초에 걸려 와 6초간 울리고, 받으면 상사가 티어 3 경보 하나를 지목해
통과시키라 또는 막으라고 지시합니다. **지시는 절반이 거짓입니다.** 지금까지
이 게임의 정보는 전부 협조적이었는데, 전화는 플레이어를 틀리게 만들 수 있는
유일한 요소입니다. 검사 41개가 붙어 있습니다.

**같은 날 시나리오 제안을 두 번 썼습니다.** 1차(내부자 찾기)는 버렸고
2차는 노동 시뮬 방향입니다. `notes/09-scenario-proposal.md`에 있습니다.

**그중 최소 규모(제안 A, 인수인계서)를 넣기로 정하고 구현했습니다.**
결과 화면 맨 위에 다음 근무자에게 넘기는 문서가 얹힙니다. 규칙은
`GAME_SPEC` 15절입니다. **표시 레이어라 난이도 실험에 영향이 없습니다** —
점수도 라이프도 경보 큐도 건드리지 않고 이미 쌓인 상태만 읽습니다.
제안 B(아침 연락)와 C(계정 잠금)는 **넣지 않았고 아직 미정입니다.**

**08-21 오후에 손 확인이 끝나고 규칙이 얼었습니다.** 런시트 17건 전부
통과했고 `eventIntervalMs` 3000ms가 확정됐습니다.

**그래서 지금 할 일은 난이도 실험 20판입니다.** 기준 3000으로 10판,
비교 2000으로 10판. 그것이 마지막 미시작 필수 요구사항입니다.
**실험 중에는 규칙 상수를 건드리면 안 됩니다.**

- 공개 주소: https://myeongjundev.github.io/mini-game/
- 저장소: https://github.com/myeongjundev/mini-game (public)
- 프로젝트 루트: `mini-game/aleph-t02-soc-shift30/`

## 지금 상태

| 항목 | 상태 |
|---|---|
| 게임 본체 (엔진·화면·저장·접근성) | 완료, 배포됨 |
| 인트로·로비 | 리뷰 **7건 중 6건 해결**, 배경 교체·정렬 완료. 남은 것은 배경 상표(m-3) |
| MEMO 방해 요소 | 완료. 사람 플레이 확인됨 |
| **상사의 전화 (14절)** | **완료, 배포됨. 손 확인 통과** |
| **시나리오 A (인수인계서)** | **완료. 손 확인 통과.** `GAME_SPEC` 15절 |
| 시나리오 B·C (아침 연락 / 계정 잠금) | 제안만 있음. 넣을지 미정 |
| 메모 로그 (근무 중 / 결과 화면) | 완료, 배포됨. **손 확인 통과** |
| 로비 반응형 | 완료. 1920에서 CRT 430px, 390에서 화면 가로의 89% |
| 경보 제한시간 | **3000ms 확정** (1400 → 2000 → 3000). 손 확인 통과, 규칙 동결 |
| 로비 가이드·설정 모달 | 완료. CRT 밖 씬 기준 900px. **손 확인 통과** |
| HOW TO PLAY 가이드 | **7쪽.** 근무 중 공지에 이어 3쪽에 상사의 전화를 넣었습니다 |
| 소리 크기 | **5단계**(20~100%). 눈금과 백분율로 보입니다. **귀 확인 통과** |
| 방향키 조작 | 로비·일시정지·결과·모달 전부. 근무 중에는 켜지지 않습니다 |
| 캐릭터 초상 8종 | 파일 검수 완료. 메모 발신자·경보 당사자에 연결됨 |
| 전화 화면 컴포넌트 | 화면만 완료. **게임에 안 붙임** — 규칙 연결은 실험 뒤 (`GAME_SPEC` 14.11) |
| **배경음 3곡** | **완료. 귀 확인 남음.** 로비·근무·라이프 1개에 각각 붙습니다 |
| 배포 산출물 | **3.5MB** (음원 3.0MB. 코드·그림은 여전히 240KB) |
| 테스트 | **289개 통과** (검사 파일 25개). **CI가 이제 실제로 돌립니다** |
| 래스터 자산 15종 | 검수 완료. 팔레트·알파·치수·형태 차분 전부 통과 |
| lint / build / tsc | 전부 통과 |
| 메모 중 판정 멈춤 버그 | 수정·검사 완료. **손 확인 통과** |
| 난이도 실험 20판 | **미시작 — 이제 막는 것이 없습니다.** 기준 3000 / 비교 2000 |
| QA 검사표 | 해상도·저장·안전·빌드 완료. 11절 MEMO **27건 중 10건**, 12절 모달 완료 |
| 증거 캡처 | **미시작** |
| 배경 이미지 상표 제거 | **미완** (`MultiSync 14`, `WINDOWS NT SECURITY`) |

공개 주소는 `main`의 최신 커밋을 따라갑니다.

**푸시는 이미 끝났습니다.** 로컬과 원격이 일치하고 커밋 안 된 변경도
없습니다. 8월 20일 작업은 전부 공개본에 올라가 있습니다.

**08-21에 순서를 바로잡았습니다.** 그전까지는 푸시가 먼저고 확인이 나중이라
검증 안 된 것이 공개돼 있었는데, 이제 손 확인을 끝내고 올립니다.

### 손 확인 — **끝났습니다 (08-21)**

런시트 17건을 사람이 전부 돌려보고 **전부 통과**했습니다. 멈춤 버그,
3000ms 체감, 무입력 9초, 일시정지 가드 둘, 전화 넷, 인수인계서, 소리
5단계, 모달 마우스 닫기, 이미지 로딩 실패까지입니다.

결과는 `notes/03-test-matrix.md`의 "손 확인 완료 — 규칙 동결" 절에 있고,
QA 검사표 13개 행이 **손 확인 통과**로 바뀌었습니다.

**그래서 규칙을 얼렸습니다.** `eventIntervalMs` 3000ms 확정입니다. 실험
20판이 끝날 때까지 `eventIntervalMs` · `VERDICT_FLASH_MS` · `MEMO` ·
`PHONE` 상수를 건드리지 않습니다. 중간에 바꾸면 앞 기록이 무효입니다.

### 남은 귀 확인 셋 — 규칙과 무관합니다

런시트를 만든 뒤에 배경음과 라이프 손실 표시가 붙었습니다.

| 볼 것 | 정상 | 왜 자동으로 못 하나 |
|---|---|---|
| **배경음 한 바퀴** | 로비 → 근무 → 라이프 1개 → 결과 → 재시작에서 곡이 겹치거나 끊기지 않는다 | **귀** |
| **효과음 균형** | 정답·오답·크리티컬이 음악 위에서 선명하다 | **귀.** 게인과 볼륨은 단위가 달라 숫자로 못 견줌 |
| **라이프 손실 표시** | 틀리면 화면이 흔들리고 그 하트가 붉게 꺼진다. **연달아 틀려도 매번** 흔들린다 | rAF |

**셋 다 표시 레이어입니다.** 실험 도중에 확인해도 기록이 무효가 되지
않습니다. 실험 20판을 돌리면서 같이 보면 됩니다.

**추적 안 된 파일은 없습니다.** `public/` 17개가 전부 커밋돼 있어
clone하면 그대로 돕니다.

배포는 `main` 푸시 시 자동입니다. 상태는 이렇게 봅니다.

```bash
gh run list --limit 3
```

## 학원 도착하면 제일 먼저

```bash
cd mini-game/aleph-t02-soc-shift30 && npm ci && npm run dev
```

`npm ci`를 빼먹지 마세요. `node_modules`는 저장소에 없습니다.
Node는 **24**를 쓰세요. vite 8이 `^20.19.0 || >=22.12.0`을 요구합니다.

## 다음에 할 일 (순서대로)

### 0. 손 확인 — **완료 (08-21)**

런시트 17건 전부 통과했습니다. 결과는 `notes/03-test-matrix.md`의
"손 확인 완료 — 규칙 동결" 절에 있습니다. 그때 함께 정해진 것 둘입니다.

- **`eventIntervalMs` 3000ms 확정.** 사람이 돌려보고 "읽고 따져볼 여유가
  충분하다"로 판단했습니다
- **무입력이면 9초쯤 FAILURE.** 옛 기록의 "4.2초"는 제한시간 1400 시절
  값이었고 이번에 다시 쟀습니다

**규칙이 얼었습니다.** 실험 20판이 끝날 때까지 `eventIntervalMs` ·
`VERDICT_FLASH_MS` · `MEMO` · `PHONE` 상수를 건드리지 않습니다.

### 0-1. 집에서 이어받기

**clone만으로 이어받을 수 있습니다.** 8월 20일 작업은 전부 `main`에
올라가 있고 로컬과 원격이 일치합니다.

```bash
git clone https://github.com/myeongjundev/mini-game.git
cd mini-game/aleph-t02-soc-shift30 && npm ci && npm run dev
```

`node_modules`는 저장소에 없으니 `npm ci`를 빼먹지 마세요. Node는 **24**입니다.

**08-21 작업은 전부 푸시됐습니다.** 배경음 3곡, 라이프 손실 표시, 로비
스크롤 수정, 가이드 7쪽, 전화기 그림 잘림 수정까지 공개본에 올라가 있습니다.

### 자산은 전부 따라옵니다

**커밋 안 된 것이 하나도 없습니다.** 미루고 있던 자산 셋을 08-20에 전부
추적하기로 정하고 넣었습니다. USB로 따로 옮길 것이 없습니다.

- `prompts/art-source/` — 초상 원본과 반려본 **약 17MB.** clone이 그만큼
  무거워지는 대신, 초상을 다시 뽑을 때 어느 기계에서든 됩니다
- `prompts/process-it-support-portrait.ps1` — 일회용 후처리 스크립트.
  **절대 경로가 박혀 있어 다른 기계에서 그대로 안 돕니다.** 입력·출력 그림이
  함께 있으니 집에서 보고 고칠지 지울지 정하세요
- password 창 webp 4장 — 둘은 쓸 것, **둘은 쓰지 않기로 한 것**입니다.
  나중에 정리합니다

무엇이 왜 있는지는 `docs/FILE_INDEX.md`의 "코드가 참조하지 않는 자산" 절에
있습니다.

배경 원본 PNG는 `fd96e26`에서 WebP로 바뀌며 지워졌지만 이력에 남아 있습니다.
다시 뽑을 일이 있으면 이렇게 꺼냅니다.

```bash
git show 969b1c4:aleph-t02-soc-shift30/public/lobby-office-blank.png > lobby-office-blank.png
```

### 1. 리뷰 지적 8건 처리 — 코드는 끝, 이미지 2건 남음

`notes/08-work-log.md`의 "리뷰 지적 사항" 절에 전문과 실측값이 있습니다.

| 담당 | 항목 | 상태 |
|---|---|---|
| 코덱스 | C-1 판단 가이드 복구 (치명적) | 완료 `e9a3531` |
| 코덱스 | M-1 11px 미만 글자 | 완료 `e9a3531` `6afa722` |
| 코덱스 | M-3 aria-live 범위 | 완료 `6afa722` |
| 코덱스 | m-1 로비 진입 포커스 | 완료 `e9a3531` |
| 클로드 | M-2 미판정 결정적 항목 | 완료 |
| 클로드 | M-5 배경 용량 줄이기 | 완료 `fd96e26` |
| 직접 | m-3 배경의 구워진 UI 제거 | **화면 비우기 완료. 상표 남음** |

코덱스가 토큰이 떨어져서 나머지는 클로드가 이어받았습니다. 담당 분할을
한 번 넘긴 것이므로, 코덱스를 다시 쓸 때 `src/` 최신 상태를 먼저 읽히세요.

m-3에서 화면을 비운 `lobby-office-blank`를 받아 반영했고, UI를 그려진
유리에 오차 0.04px로 맞췄습니다. **실제 상표(`MultiSync 14`,
`WINDOWS NT SECURITY`)만 아직 그림에 남아 있습니다.**

배경을 다시 뽑을 일이 있으면 두 가지를 같이 해주세요.

- 상표를 가상 이름으로 교체
- **모니터를 지금보다 크게** — 유리가 씬 높이의 52% 이상(지금 39.21%)이면
  좁은 화면에서 배경을 확대하지 않아도 됩니다. 확대는 책상 가장자리를
  잘라내므로 없는 편이 낫습니다

비율 1672:941(16:9)은 유지해주세요. 바뀌면 좌표를 전부 다시 잡아야 합니다.
좌표를 재고 반영하는 절차는 `docs/DESIGN_TOKENS.md` 9절에 있습니다.

### 2. 게임 동작 확인 5가지 — **전부 통과**

브라우저에서 직접 확인했습니다. 결과와 근거는 `notes/03-test-matrix.md`에
있습니다.

| 검사 | 결과 |
|---|---|
| 30초 타이머 정확도 | 통과. 비율 0.975~0.994 |
| 미판정 시 라이프 감소 | 통과. 라이프 3→0, COMBO ×0, SCORE 유지 |
| 무입력이면 반드시 패배 | 통과. FAILURE. 당시 제한시간 1.4초 기준 4.2초. **3초로 올린 뒤 재측정 필요(계산상 9.0초)** |
| **재시작 반복 시 타이머 가속** | 통과. 7판 연속 비율 1.00 유지 |
| 탭 전환 자동 일시정지 | 통과. 자동 재개하지 않음 |

미판정이 정확도를 오염시키지 않는 것도 섞인 판으로 확인했습니다.
판정 3장 전부 정답 + 미판정 3장에서 ACCURACY 100.0%입니다.

### 3. 난이도 실험 20판

**게임 규칙이 확정된 뒤에 시작하세요.** 중간에 바꾸면 앞 기록이 무효입니다.
지금은 제한시간 2000ms와 메모 로그가 손 검증 전이므로 **0번을 먼저 끝내야
합니다.**

MEMO 슬롯은 경과 시간(3 / 7 / 12 / 16초) 기준이라 `eventIntervalMs`를 바꿔도
배치가 그대로입니다. 실험에 변수가 섞이지 않도록 그렇게 설계했습니다.
`VERDICT_FLASH_MS`와 `MEMO` 상수는 20판 내내 고정하세요.

**비교값이 아직 정해지지 않았습니다.** 원래 1400 → 1100(더 어렵게)이었는데,
플레이해보니 시간이 모자란 쪽이 문제라 기준값이 두 번 올랐습니다.
1400 → 2000은 제가 재서, 2000 → 3000은 동기 피드백을 받아서입니다.
**비교값도 이걸로 정해졌습니다.**

- 기준값 `eventIntervalMs` **3000**으로 10판 → `records/playtest-before.csv`
- 비교값 **2000**으로 10판 → `records/playtest-after.csv`
- 근거는 `notes/05-difficulty-experiment.md`에 있습니다
- 한 판 끝날 때마다 바로 적으세요. SHIFT LOG를 보면 `failure_reason`이 바로 나옵니다
- 방법은 `notes/07-evidence-plan.md`

### 3-1. 실험이 끝나야 손댈 수 있는 것 두 가지

둘 다 데이터셋이나 규칙을 건드려서 실험 중에는 못 바꿉니다.

**티어 3의 정답 쏠림.** 티어 3이 ALLOW 1 / BLOCK 4라 **다 막으면 80%가
맞습니다.** 티어 1·2는 60%인데 티어 3에만 몰려 있습니다. 티어는 카드에
대놓고 표시되고 크리티컬 경고음도 `tier === 3`에 걸려 있어서, 그 소리가
"이제부터 막아"라는 신호로 쓰일 수 있습니다. 심각도 93%, 표시 개수 93%와
같은 계열입니다. 근거와 고치는 방향은 `docs/ALERT_DATASET.md`의
"티어에도 같은 누수가 있다" 절에 있습니다.

**상사의 전화.** 규칙은 `docs/GAME_SPEC.md` 14절에 다 썼습니다. 받는 시간을
넘기면 라이프 -1이고, 진짜 게임은 상사의 지시를 따를지 정하는 쪽입니다.
라이프를 잃는 경로가 늘면 난이도가 근본적으로 바뀌므로 실험 뒤입니다.

두 가지가 서로 얽혀 있습니다. 전화 설계가 티어 3 분포 위에 서 있어서
(지시를 "통과시켜" 하나로 고정 못 한 이유), **티어 3을 고치면 14.4의
참·거짓 표도 다시 봐야 합니다.**

**초상 표도 같이 봐야 합니다.** 얼굴이 붙은 경보 넷은 ALLOW 2 / BLOCK 2로
맞춰져 있는데, ALLOW 쪽 하나가 티어 3의 `backup-job`입니다. 티어 3을
재배치하면서 이 카드를 옮기면 균형이 깨집니다.
`src/game/data/portraits.test.ts`가 실패로 알려주지만, **고칠 때 얼굴을
어디로 옮길지는 사람이 정해야 합니다.**

### 4. QA 검사표 + 증거 캡처

해상도·저장복구·공개안전·빌드는 이미 실행해 `notes/03-test-matrix.md`에
기록했습니다. **남은 것은 게임 루프가 도는 검사뿐이고, 표에 `(사람)`으로
표시해뒀습니다.**

- 검사 절차: `aleph-t02-soc-shift30/docs/QA_CHECKLIST.md`
- 기록처: `notes/03-test-matrix.md`
- 캡처 계획: `notes/07-evidence-plan.md` (파일명까지 정해져 있음)

QA 9.4(저장소 공개 범위)는 **정리됐습니다.** `notes/`와 `records/`는
공개하는 쪽으로 정하고 검사표를 고쳤습니다. 민감정보 검색 0건이고 설계
기록은 T02에서 감출 것이 아니라 보여야 할 자산이라는 판단입니다.
`evidence/`만 `.gitignore`로 계속 제외합니다 — 캡처에 무엇이 찍힐지는
찍어봐야 알기 때문입니다. 근거는 `notes/03-test-matrix.md`에 있습니다.

## 작업 분할 규칙

파일이 겹치면 서로의 작업을 덮어씁니다. 지금까지 이 규칙으로 굴러왔습니다.

| 담당 | 파일 |
|---|---|
| 코덱스 | `src/` 구현, 컴포넌트 테스트 |
| 클로드 | `docs/`, `notes/`, `BUILD_ORDER.md`, git, 브라우저 검증 |
| 공통 금지 | 상대 담당 파일에 손대지 않기 |

**이 분할은 현재 중단 상태입니다.** 코덱스가 토큰이 떨어져 `6afa722` 이후로는
클로드가 `src/`까지 전부 맡았습니다. 코덱스를 다시 쓸 때는 `src/` 최신
상태를 먼저 읽히세요. 특히 `App.tsx` · `useGameLoop.ts` · `machine.ts`가
MEMO 때문에 많이 바뀌었습니다.

## 알아둘 것

- **`git checkout --`를 함부로 쓰지 마세요.** 커밋 안 된 상대 작업이 날아갑니다. 한 번 겪었습니다
- **`perl -0pi` / `sed -i`로 파일을 고치지 마세요.** 저장소가 CRLF라 `\n`
  패턴이 어긋나 파일이 깨집니다. **세 번 겪었습니다.** 편집 도구를 쓰세요
- 배포는 `main` 푸시 시 자동입니다. 푸시하면 공개 주소가 바로 바뀝니다
- `site/`는 빌드 산출물이라 Git에서 제외돼 있습니다. 직접 편집하지 마세요
- `evidence/`도 제외돼 있습니다. 증거 원본은 로컬과 제출 PDF에만 두세요
- 저장소가 **public**입니다. 개인정보가 들어가지 않게 주의하세요

## 미해결 위험

1. **판이 멈추던 버그를 고쳐 커밋했지만 손 확인은 아직입니다.**
   메모가 떠 있는 동안 `A`·`D`를 누르면 그 경보가 판정도 만료도 되지 않아
   근무 시계만 흐르는 상태가 됐습니다. 원인과 수정은
   `notes/08-work-log.md`의 해당 절과 `docs/TROUBLESHOOTING.md` 1-1에 있습니다.
   회귀 검사 4개를 붙였고 수정 전으로 되돌려 3개가 실패하는 것까지 확인했지만,
   **rAF가 자동화 브라우저에서 안 돌아 실물 확인은 사람이 해야 합니다.**
   메모가 뜬 순간 `A` → `SPACE` → `A` 순서로 눌러보면 됩니다
2. **메모 로그와 제한시간 3000ms가 손 검증 전입니다.** MEMO 본체는 사람이
   돌려봤지만 마지막 세 변경(메모 로그, 결과 화면 공지 연결, 3000ms)은
   아직입니다. QA 11절 **27건 중 17건이 미실행**이고 전부 사람 몫입니다.
   문제가 있으면 `git revert`보다 고쳐서 다시 푸시하는 편이 빠릅니다
3. 배경 이미지에 실제 상표(`MultiSync 14`, `WINDOWS NT SECURITY`)가 읽힙니다.
   배경을 다시 뽑을 때 가상 이름으로 바꾸세요
4. **`public/`에 쓰면 안 되는 그림 두 장이 들어 있습니다.**
   `password-locked.webp`와 `password-unlocked.webp`는 물리 장치로 잘못 만든
   초기안입니다. 지우지 않고 둔 것은 같은 실수를 되풀이하지 않기 위해서인데,
   **`public/`에 있으니 무심코 쓸 위험이 있습니다.** 패스워드 방해 요소를
   구현할 때 `password-window-*` 쪽인지 반드시 확인하세요
5. **`prompts/process-it-support-portrait.ps1`은 그대로 돌지 않습니다.**
   절대 경로가 박혀 있고 초상 한 장에만 썼습니다. 고쳐 쓰거나 지우세요.
   후처리 절차 자체는 `prompts/05` 8절에 글로 남아 있습니다

## 해소된 위험

기록으로 남깁니다. 같은 걱정을 다시 하지 않기 위해서입니다.

- ~~`.crt-display::after` 스캔라인이 `DESIGN_TOKENS` 8절을 어김~~ —
  **이미 없어져 있었습니다.** `9adecab`에서 들어왔다가 `969b1c4`(배경 교체)에서
  함께 사라졌습니다. 고친 줄 모르고 미해결로 남겨두고 있었습니다
- ~~QA 9.4가 실제와 어긋남~~ — `efbbc1b`에서 **검사표를 고치는 쪽으로**
  정했습니다. `notes/`와 `records/`는 공개하고 `evidence/`만 제외합니다.
  민감정보 검색 0건이고, 설계 기록은 T02에서 감출 것이 아니라 보여야 할
  자산이라는 판단입니다

- ~~`useGameLoop` 프레임 구동 이상~~ — **제품 문제가 아니었습니다.** 재시작
  7판을 실측해 게임 시간이 실제 시간을 0.975~0.994 비율로 따라가는 것을
  확인했습니다. 차이는 판 길이와 무관하게 0.1초로 일정하며 측정 방식의
  오차입니다. 단위 테스트에서 rAF 21프레임에 100ms만 흐르던 것은 **테스트
  환경의 특성**입니다. 근거는 `notes/03-test-matrix.md`
- ~~인트로 실시간 관측 불가~~ — 사람이 직접 확인했습니다. 부팅 로그 4줄과
  타이틀 카드가 정상 재생됩니다. 자동화로는 4단계의 DOM과 크기를 확인했고,
  라이브 리전이 단계마다 갱신되는 것까지 봤습니다
- ~~로비를 눈으로 본 사람이 없음~~ — 확인했습니다. UI가 그려진 모니터 화면
  안에 자연스럽게 들어갑니다
- ~~메모 중 판정이 영구히 막힘~~ — `activeMemo !== null`이 원인이었습니다.
  HMR로 코드만 갱신되면 리듀서 상태에 그 필드가 없어 `undefined !== null`이
  참이 됩니다. 참/거짓 판정으로 바꾸고 회귀 테스트를 붙였습니다 (`dfd6d2f`)
- ~~메모가 뜬 경보는 피할 수 없는 미판정~~ — 경보 수명이 1.4초인데 메모를
  읽고 닫는 데만 그 이상이 듭니다. 메모 중 경보 제한시간을 멈추고, 메모를
  경보 카드 위로 옮기고, 판정 버튼을 비활성으로 남겼습니다 (`49757df`).
  **사람의 플레이 확인은 아직입니다**

## 문서 지도

| 찾는 것 | 파일 |
|---|---|
| 단계별 작업 지시서 | `aleph-t02-soc-shift30/BUILD_ORDER.md` |
| 게임 규칙 원본 | `aleph-t02-soc-shift30/docs/GAME_SPEC.md` |
| 경보 15개 데이터 | `aleph-t02-soc-shift30/docs/ALERT_DATASET.md` |
| 색·폰트 규칙 | `aleph-t02-soc-shift30/docs/DESIGN_TOKENS.md` |
| 게임 루프 명세 | `aleph-t02-soc-shift30/docs/GAME_LOOP_SPEC.md` |
| 저장·복구 명세 | `aleph-t02-soc-shift30/docs/STORAGE_AND_RECOVERY.md` |
| QA 절차 | `aleph-t02-soc-shift30/docs/QA_CHECKLIST.md` |
| 과제 제출 기록 | `notes/01` ~ `notes/07` |
| 지금까지의 경과 | `notes/08-work-log.md` |
