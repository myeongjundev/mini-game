# SOC SHIFT:30

Vite, React 18, TypeScript 기반의 브라우저 미니게임 프로젝트다.

## 실행

```sh
npm install
npm run dev
```

## 스크립트

- `npm run dev`: 개발 서버 실행
- `npm run build`: 타입 검사 후 `../site`에 프로덕션 빌드
- `npm run lint`: ESLint 검사
- `npm run preview`: 프로덕션 빌드 미리보기
