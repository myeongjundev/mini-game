/**
 * 판정 결과를 화면에 띄워두는 시간.
 * 난이도 실험의 통제 대상이 아니다. 전·후 20판 내내 이 값을 고정한다.
 * eventIntervalMs보다 반드시 짧아야 한다. 그렇지 않으면 판정 표시가 끊기지 않는다.
 */
export const VERDICT_FLASH_MS = 800

/**
 * 라이프를 잃은 하트가 꺼지는 표시를 띄워두는 시간.
 *
 * 판정 표시(`VERDICT_FLASH_MS`)보다 짧아야 한다. 판정 표시가 사라진 뒤에도
 * 하트가 붉게 남아 있으면 다음 경보의 결과처럼 읽힌다.
 *
 * 난이도 실험의 통제 대상이 아니다. 전·후 20판 내내 고정한다.
 */
export const LIFE_FLASH_MS = 400

/**
 * eventIntervalMs는 경보 한 장의 제한 시간이다. 고정 박자가 아니라 상한이고,
 * 먼저 판정하면 다음 경보가 즉시 온다.
 *
 * 3000ms는 사람이 읽는 속도에서 나왔다. 경보 한 장에 사실이 4줄이고 줄마다
 * 라벨과 값을 읽고 표시가 붙었는지까지 봐야 한다. 2000ms면 한 줄에 500ms이고,
 * **2000ms를 배포한 뒤에도 "읽을 시간이 촉박하다"는 플레이 피드백이 왔다.**
 * 3000ms면 한 줄에 750ms가 된다.
 *
 * 대신 봉지와의 정확한 일치를 포기했다. 티어 구간이 10초이므로 2000ms에서는
 * 구간당 5장이 뽑혀 티어당 경보 5장과 딱 맞았고 한 판에 15장, 즉 데이터셋
 * 전체를 봤다. 3000ms에서는 구간당 3.3장이라 티어당 5장 중 1~2장을 못 보고
 * 지나간다. 빠지는 장이 판마다 달라서 두 번째 판이 첫 판과 달라진다.
 *
 * 한 판에 10장이면 판정 표본이 그만큼 작아진다. 정확도 눈금이 판정 1개당
 * 10%p이므로 난이도 실험 20판에서 이보다 작은 차이는 잡음에 묻힌다.
 * **여기가 하한이다.** 3500ms는 한 판 8.6장에 눈금 11.6%p라 실험이 차이를
 * 못 재고, 티어 3이 2.9장이라 후반 난이도 상승도 사라진다.
 *
 * 1400ms에서는 구간당 7장이 뽑혀 봉지가 7초에 비고 같은 경보가 두 번 나왔다.
 * 늘리는 방향에서는 반복이 생기지 않는다.
 */
export const DIFFICULTY = {
  eventIntervalMs: 3000,
  totalTimeMs: 30_000,
  lives: 3,
} as const

/**
 * 근무 중 방해. 규칙은 `docs/GAME_SPEC.md` 13절이다.
 * 난이도 실험 중에는 이 값을 고정한다.
 *
 * slotsMs는 발화 시각이 아니라 **자격 시각**이다. 실제 표시는 그 시각을
 * 지난 뒤 새 경보가 뜨는 순간이다. 경보가 끝나갈 때 끼어들면 아무리 빨리
 * 닫아도 미판정이 되어 실력으로 피할 수 없다.
 */
/**
 * 상사의 전화. 규칙은 `docs/GAME_SPEC.md` 14절이다.
 * 난이도 실험 중에는 이 값을 고정한다.
 *
 * slotMs가 21초인 근거. eventIntervalMs가 3000이라 경보는 0·3·6…초에 뜨고
 * 메모는 3·9·12·18초에 발화한다. 21초는 메모 넷 어디와도 겹치지 않고,
 * 벨 6초가 27초에 끝나 근무 종료 3초 전에 결판난다. 티어 3 구간이라
 * 지목할 경보가 봉지에 남아 있다.
 *
 * **24초는 쓸 수 없다.** 벨이 30초를 넘겨 근무 종료와 동시에 라이프가
 * 깎이고, 이는 14.5의 공정성 규칙을 어긴다.
 */
export const PHONE = {
  perShift: 1,
  slotMs: 21_000,
  ringMs: 6_000,
} as const

export const MEMO = {
  perShift: 4,
  slotsMs: [3_000, 7_000, 12_000, 16_000],
  readThresholdMs: 600,
} as const
