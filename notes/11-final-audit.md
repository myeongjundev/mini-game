# 최종 감사 — 요구사항 매트릭스

기준 문서: `aleph-t02-soc-shift30/prompts/09_ALEPH_ASSIGNMENT_2_FINAL_AUDIT_HANDOFF.md`
검사 일자: 2026-08-21 (캡처 대조 갱신 2026-08-21)
검사 대상: 공개본 `https://myeongjundev.github.io/mini-game/`
번들: `assets/index-C1AyqaR2.js` — 재도전 버튼까지 반영된 현재 배포본.
`RETRY SHIFT` 문자열이 번들에 있는 것을 내려받아 확인했습니다.

캡처 원본은 `실행 이미지/` 17장입니다. `.gitignore`로 제외돼 있어
저장소에는 올라가지 않습니다.

판정은 네 가지만 씁니다. `통과` / `미통과` / `증거 부족` / `해당 없음`.
**파일이 있다거나 과거 문서에 완료라고 적혀 있다는 이유로 통과 처리하지
않았습니다.** 자동 검사는 로직 증거이고 화면·감각 항목의 대체물이 아닙니다.

---

## A. 한 판의 완결성과 상태 표시

| 요구사항 | 필요한 증거 | 관찰한 증거 | 판정 | 부족한 점 | 다음 행동 |
|---|---|---|---|---|---|
| 공개 주소에서 한 판을 시작·종료·재시작 | 실제 플레이 | 손 확인 17건 전부 통과(`03-test-matrix.md`). **캡처 6장으로 뒷받침**(E01·E02 전후·E03·E04·E05) | 통과 | | |
| 30초 안에 성공 또는 실패 도달 | 실측 | 30초 타이머 비율 0.975~0.994, 무입력 시 9초 FAILURE | 통과 | | |
| 조작·상태·성공·실패가 화면에서 구분 | 화면 | `TIME`·`SCORE`·`SECURITY`·`COMBO` 상시 표시, `SHIFT COMPLETE` / `SECURITY LOST` | 통과 | | |
| 재시작 시 이전 판 값이 남지 않음 | 검사 | `restartGame`이 초기 상태를 새로 만든다. 재시작 7판 실측 비율 1.00 유지 | 통과 | | |
| 규칙 문서가 현재 동작과 일치 | 대조 | `GAME_SPEC` 13·14·15절이 구현과 일치. 가이드 7쪽에 전화·인수인계서 반영 | 통과 | | |
| 로컬 증거 캡처 | 이미지 | **6장 확보.** E01 로비(조작키 노출) · E02 판정 전후(`SCORE` 0→100, `COMBO` ×0→×1) · E03 `SHIFT COMPLETE` 4,900 · E04 `SECURITY LOST` · E05 재도전 직후(29.5s·♥♥♥·0·×0) | 통과 | E03·E04는 재도전 버튼 배포 **직전** 빌드라 버튼이 한 개다 | 신경 쓰이면 E04만 다시(3번 틀리면 4초) |

## B. 입력과 반응형 화면

| 요구사항 | 필요한 증거 | 관찰한 증거 | 판정 | 부족한 점 | 다음 행동 |
|---|---|---|---|---|---|
| 1366×768 가로 넘침 0 | 콘솔 실측 | **공개본에서 0.** 재측정에서 `clientWidth`가 `innerWidth`와 같아(1366) **자리를 차지하는 스크롤바가 없음**도 확인. E06에 뷰포트 `1366 × 768` 찍힘 | 통과 | 캡처에 크기는 있으나 **콘솔 값이 없음** | 콘솔 한 줄 포함해 재촬영하면 캡처만으로 완결 |
| 1920×1080 가로 넘침 0 | 콘솔 실측 | **공개본에서 0**, 세로 넘침도 0. `clientWidth` = `innerWidth` = 1920. E07에 뷰포트 `1920 × 1080` 찍힘 | 통과 | 위와 같음 | 위와 같음 |
| 조작 영역이 잘리지 않음 | 화면 | 08-21에 로비 메뉴 넘침을 고침. 320~1920 아홉 폭에서 넘침 0, 여유 5~95px | 통과 | | |
| 연속 입력·키 누르고 있기·반대 키 | 검사 | `event.repeat` 가드 + `resolvedRef` 잠금. 자동 검사 통과 | 통과 | 사람 연타 미실행 | 사용자가 20회 연타 확인 |
| 키보드와 마우스 모두 조작 가능 | 화면 | 판정·공지 닫기·전화 받기 모두 버튼이 있고 키도 있다. 모바일 터치 완주 가능 | 통과 | | |

## C. 포커스·일시정지·장시간 안정성

| 요구사항 | 필요한 증거 | 관찰한 증거 | 판정 | 부족한 점 | 다음 행동 |
|---|---|---|---|---|---|
| 포커스 이탈 시 시간이 불공정하게 흐르지 않음 | 실측 | 탭 전환 자동 일시정지, 자동 재개하지 않음 | 통과 | | |
| 일시정지·재개가 중복 타이머를 만들지 않음 | 실측 | 재시작 7판 연속 비율 1.00. 리스너 중복 등록 검사 통과 | 통과 | | |
| **일시정지 중 상태가 바뀌지 않음** | 검사 + 실측 | 08-21에 결함을 고침 — 일시정지 중 전화가 받아지고 공지가 닫혔다. 손 확인 통과 | 통과 | | |
| **10분 연속 실행** | 실행 기록 | **공개본에서 648초 연속 실행.** 128판을 재시작하며 2,517회 입력을 처리했고 종료 시점 콘솔 로그·빨간 오류 0건. `E14-runtime-10min-result.json`과 종료 화면 PNG 보관 | 통과 | | |
| 콘솔 빨간 오류 0건 (로드) | 콘솔 | **공개본에서 0건.** E11·E12·E13 세 장 모두 콘솔이 비어 있고 `No Issues` | 통과 | | |
| 콘솔 0건 (플레이·재시작 뒤) | 콘솔 | **캡처 2장.** E12-console-play(결과 화면) · E12-console-restart(재시작 뒤 `TIME 29.0s`·♥♥♥·`SCORE 0`). 둘 다 콘솔 비어 있고 `No Issues` | 통과 | | |

## D. 난이도 실험

| 요구사항 | 필요한 증거 | 관찰한 증거 | 판정 | 부족한 점 | 다음 행동 |
|---|---|---|---|---|---|
| 변경 전 10회 + 변경 후 10회 기록 | CSV | **실제 20판 완료.** `records/playtest-before.csv`는 3000ms 10판, `records/playtest-after.csv`는 2000ms 10판이며 두 묶음 사이에서 `eventIntervalMs` 하나만 변경 | **통과** | | 최종값 3000ms 유지 |

**2026-08-21에 실제 20판을 완료했습니다.** 3000ms와 2000ms를 각각 10판씩
기록했고 원하는 결과를 골라내지 않았습니다. 두 조건 모두 성공 0회였지만,
3000ms에서 생존 시간 중앙값이 7.05초 → 13.75초, 정확도 중앙값이
72.5% → 83.75%, 점수 중앙값이 2,300 → 3,950으로 개선됐습니다.

최종값은 **3000ms**입니다. 두 조건 모두 혼합 실패가 10회 반복됐다는 한계는
숨기지 않습니다. 원자료와 전체 비교는 `records/*.csv`와
`notes/05-difficulty-experiment.md`에 있습니다.

## E. 초기화·저장·손상 복구

| 요구사항 | 필요한 증거 | 관찰한 증거 | 판정 | 부족한 점 | 다음 행동 |
|---|---|---|---|---|---|
| 새 게임에서 현재 판 값 초기화 | 검사 | 자동 검사 통과 | 통과 | | |
| 최고 점수·접근성 설정 유지 | 검사 | `storage.ts`가 보존. 자동 검사 통과 | 통과 | 캡처 없음 | 사용자가 새로고침 전후 캡처 |
| 진행 상태는 보존하지 않음 | 검사 | 저장 대상에 판 상태가 없다 | 통과 | | |
| 손상값에서 기본값으로 시작 | 브라우저 실측 | **공개본에서 직접 확인.** 잘못된 JSON · 버전 불일치(v99) · 잘못된 자료형 · 범위 밖 숫자를 넣고 새로고침 → 전부 정상 렌더, `SOUND // OFF` · `REDUCE MOTION // OFF` 기본값으로 복구 | 통과 | | |
| 손상 복구 시 콘솔 오류 0건 | 콘솔 | 복구 중 빨간 오류 0건 | 통과 | | |

## F. 효과와 접근성 선택권

| 요구사항 | 필요한 증거 | 관찰한 증거 | 판정 | 부족한 점 | 다음 행동 |
|---|---|---|---|---|---|
| 음소거를 켜면 즉시 소리가 멈춤 | 실측 | 첫 로드가 음소거이고 토글이 즉시 반영. 음소거 중에는 음원을 받지도 않는다(요청 0건). **E08 후 장면에서 브라우저 탭에 스피커 아이콘이 떴다** — 라벨이 아니라 실제로 소리가 나갔다는 증거다 | 통과 | 음량 균형은 귀 확인(QA 17.15) | 사용자가 소리 켜고 확인 |
| 움직임 줄이기 즉시 반영 | 브라우저 실측 | **공개본에서 직접 확인.** 토글 시 `data-reduce-motion`이 false↔true, 라벨도 함께 바뀐다 | 통과 | | |
| 효과를 줄여도 판정·성패 규칙은 그대로 | 실측 | 라이프 손실 표시가 `screen-shake-b` → `reduced-border-flash`로 **대체**된다. 없어지지 않는다 | 통과 | | |
| 효과 작동 화면 증거 | 이미지 | **3장 확보.** E08 전후로 `SOUND // OFF` → `// ON`(음량 막대도 함께 켜짐) · E09 후 장면에서 `REDUCE MOTION // ON` | 통과 | 움직임 토글의 **직전** 장면은 별도 파일이 없다 | E08 전 장면이 같은 설정창에서 `REDUCE MOTION // OFF`를 보여주므로 **그 장이 직전 상태를 겸한다** |

## G. 공개 안전

| 요구사항 | 필요한 증거 | 관찰한 증거 | 판정 | 부족한 점 | 다음 행동 |
|---|---|---|---|---|---|
| 배포 파일에 개인정보·비밀값 0건 | 번들 검색 | **공개본 번들을 내려받아 검색.** 이메일 0건, 사용자 경로 0건, 실명 0건 | 통과 | | |
| 토큰·API 키 0건 | 번들 검색 | 두 건이 걸렸으나 **둘 다 React 내부 문자열** — `__SECRET_INTERNALS_DO_NOT_USE...`(React 내보내기 이름)와 `password:!0`(input type 목록). 비밀값 아님 | 통과 | | |
| 외부 도메인 요청 0건 | 네트워크 | **공개본에서 0건.** 1366·1920 양쪽에서 확인. **E13에 `Domain` 열을 켠 네트워크 목록**이 있고 5건 전부 `myeongjundev.github.io`다 | 통과 | | |
| 공개 페이지·자산·새로고침 정상 | 응답 | index 200, 음원 3곡 200, 새로고침 정상. E13에서 5건 전부 200/304 | 통과 | | |
| 캡처에 개인정보 없음 | 이미지 | **17장 전부 육안 대조.** 시크릿 모드로 찍어 계정 화면·북마크바·다른 탭 제목이 없고, `C:\Users\...` 경로와 토큰도 없다 | 통과 | | |

## H. 최종 제출물

| 요구사항 | 필요한 증거 | 관찰한 증거 | 판정 | 부족한 점 | 다음 행동 |
|---|---|---|---|---|---|
| 검증 안내서 **3단계 이내** | 문서 | `06-submission-plan.md`를 **5단계 → 3단계**로 다시 씀 | 통과 | | |
| AI 사용 기록 **정확히 3줄** | 문서 | 한 줄에 여러 문장이던 초안을 **한 문장씩 3줄**로 확정 | 통과 | | |
| 증거 캡처·기록표는 비공개 보관 | 위치 | `evidence/`와 `실행 이미지/` 둘 다 `.gitignore`로 제외, `records/`·`notes/`는 공개. 캡처 17장은 `실행 이미지/`에 있고 저장소에 올라가지 않는다 | 통과 | | |

---

## 요약

| 판정 | 개수 |
|---|---|
| 통과 | 35 |
| 증거 부족 | 0 |
| 미통과 | 0 |

35개 행입니다.

**35개 요구사항이 모두 통과했습니다.** 2026-08-21 공개본에서 648초 동안
128판을 연속 실행하고 2,517회 입력을 처리했습니다. 종료 시점 브라우저 콘솔
로그와 빨간 오류는 모두 0건이었습니다. 원자료는
`실행 이미지/E14-runtime-10min-result.json`과 종료 화면 PNG입니다.

난이도 실험도 실제 20판 기록으로 통과했습니다. 남은 일은 최종 PDF와 제출
화면의 내용을 교차 확인하고 사용자가 제출을 승인하는 것입니다.

### 캡처로 닫은 항목

| 항목 | 캡처 |
|---|---|
| A 실행 캡처 | E01 · E02 전후 · E03 · E04 · E05 |
| C 콘솔 0건 (로드·플레이·재시작) | E11 · E12 두 장 · E13 |
| F 효과 작동 | E08 전후 · E09 후 |
| G 외부 도메인 0건 | E13 (`Domain` 열, 5건 전부 같은 도메인) |
| G 캡처에 개인정보 없음 | 17장 육안 대조 |
| H 비공개 보관 | `실행 이미지/`가 `.gitignore`에 있음 |

### 캡처가 있으나 더 좋아질 수 있는 것

- **E06 · E07** — 뷰포트 크기는 찍혔지만 콘솔의 넘침 값이 없습니다.
  판정은 별도 실측으로 이미 통과입니다.
- **E03 · E04** — 재도전 버튼 배포 직전 빌드라 결과 화면 버튼이 하나입니다.
- 기존 **E14-memory-*** 두 장은 3분치라 사용하지 않습니다. 최신 증거는
  `E14-runtime-10min-result.json`과 `E14-runtime-10min-console-zero.png`입니다.

캡처 파일명은 `07-evidence-plan.md`에 정해져 있습니다.
