# PC 숨은 검사표

실제 실행 전 `예상 결과`를 먼저 작성하고, 실행 후 `실제 결과`와 `수정 여부`를 기록합니다.

이 표가 제출 증거의 원본입니다. 항목별 상세 절차와 확인 방법은 `aleph-t02-soc-shift30/docs/QA_CHECKLIST.md`에 있습니다.

| 검사 조건 | 예상 결과 | 실제 결과 | 수정 여부 | 증거 |
|---|---|---|---|---|
| 빠른 연속 입력 | 입력 횟수만큼만 반영되고 중복 이벤트가 없다 | 미실행 | 미정 | |
| 키를 3초간 누르고 있기 | `event.repeat`가 무시되어 판정이 1회만 일어난다 | 미실행 | 미정 | |
| 같은 경보에 A 직후 D | 첫 입력만 반영되고 두 번째는 무시된다 | 미실행 | 미정 | |
| 1366×768 | 조작 영역 잘림과 가로 넘침이 없다 | 미실행 | 미정 | |
| 1920×1080 | 화면 비율과 조작 영역이 유지된다 | 미실행 | 미정 | |
| 1366↔1920 실행 중 변경 | 현재 판이 깨지거나 초기화되지 않는다 | 미실행 | 미정 | |
| 창·탭 포커스 이탈 | 자동 일시정지되어 불리하게 진행되지 않는다 | 미실행 | 미정 | |
| 포커스 복귀 | 자동 진행하지 않고 사용자가 재개한다 | 미실행 | 미정 | |
| 수동 일시정지·재개 | 시간과 오브젝트가 중복 진행되지 않는다 | 미실행 | 미정 | |
| 다시 시작 반복 | 이벤트와 게임 루프가 중복 등록되지 않는다 | 미실행 | 미정 | |
| 10분 연속 실행 | 메모리 증가·속도 저하·오류가 없다 | 미실행 | 미정 | |
| 콘솔 검사 | 로드·플레이·재시작 후 빨간 오류 0건 | 미실행 | 미정 | |
| 음소거 토글 | 누른 즉시 반영되고 다음 판까지 기다리지 않는다 | 미실행 | 미정 | |
| 움직임 줄이기 토글 | 즉시 반영되고 화면 흔들림이 사라진다 | 미실행 | 미정 | |
| 움직임 줄이기 ON 상태의 판정 | 색 플래시로 대체되어 정보 손실이 없다 | 미실행 | 미정 | |
| 네트워크 탭 | 외부 도메인 요청 0건 | 미실행 | 미정 | |
| 흑백으로 보기 | 카테고리 아이콘 5종이 실루엣만으로 구별된다 | 미실행 | 미정 | |
| 제한시간까지 미입력 | 미판정으로 처리되어 라이프가 1 줄어든다 | 미실행 | 미정 | |
| 30초 내내 미입력 | 라이프가 소진되어 반드시 실패로 끝난다 | 미실행 | 미정 | |
| 미판정 후 결과 화면 | 검토한 경보 수와 정확도에 포함되지 않고 미판정으로만 집계된다 | 미실행 | 미정 | |
| 일시정지 중 제한시간 | 막대가 멈추고 재개하면 멈춘 지점부터 이어진다 | 미실행 | 미정 | |
| 일시정지 화면 다시 시작 | READY로 돌아가고 현재 판 값이 초기화된다 | 미실행 | 미정 | |

가로 넘침은 콘솔에서 아래 값이 `0`이면 통과입니다.

```js
document.documentElement.scrollWidth - document.documentElement.clientWidth
```

## 추가 경계 검사

| 검사 | 기대 결과 | 실제 결과 |
|---|---|---|
| 게임 시작 전 핵심 키 입력 | 게임 상태가 임의로 진행되지 않는다 | 미실행 |
| 종료 후 핵심 키 입력 | 점수와 판정이 더 변하지 않는다 | 미실행 |
| 버튼 빠른 두 번 클릭 | 게임 루프가 하나만 시작된다 | 미실행 |
| 화면 밖 포인터 입력 | 예외가 발생하지 않는다 | 미실행 |
| 제한시간 만료 직전 판정 | 판정과 미판정이 이중으로 집계되지 않는다 | 미실행 |
